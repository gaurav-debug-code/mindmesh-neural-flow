
import { FocusMode as FocusModeComponent } from "@/components/FocusMode";

const FocusMode = () => {
  return (
    <div className="container mx-auto py-6">
      <h1 className="text-3xl font-bold mb-6 neural-text">Focus Mode</h1>
      <FocusModeComponent />
    </div>
  );
};

export default FocusMode;
